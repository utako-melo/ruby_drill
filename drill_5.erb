price = 300

def calculate_price_with_tax
  tax = 0.1
  return price + price * tax
end

calculate_price_with_tax

# 1 priceがメソッド外で定義されているため、メソッド内のpriceに値が入っていない状態になっている



price = 300

def calculate_price_with_tax(price)
  tax = 0.1
  return price + price * tax
end

calculate_price_with_tax(price)